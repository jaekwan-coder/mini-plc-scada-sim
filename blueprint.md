# 미니 PLC-PC SCADA 시뮬레이션 — 프로젝트 청사진

이 문서는 이 프로젝트를 진행하며 항상 돌아와서 참고하는 기준 문서다.
막히거나 헷갈릴 때는 여기부터 다시 읽는다.

---

## 1. 이 프로젝트가 뭔지 한 줄로

**PLC 하드웨어 없이, 파이썬 코드만으로 "SCADA 시스템"의 핵심 동작(제어→수집→통계→저장)을 축소 재현하는 프로젝트.**

- 실제 SCADA: PC 프로그램에서 버튼을 누르면 → PLC를 돌리고 → 결과를 실시간으로 받아 화면에 보여주고 → 통계를 내고 → 저장한다.
- 이 미니 프로젝트: PC 프로그램(파이썬 GUI)에서 버튼을 누르면 → 가짜 PLC 로직(파이썬 클래스)이 돌고 → 결과를 화면에 보여주고 → csv에 쌓인 기록으로 통계를 내고 → 파일로 저장한다.

**구조는 100% 동일하고, "PLC가 진짜 하드웨어냐 코드로 흉내낸 것이냐"만 다르다.**

---

## 2. 왜 파이썬만으로 해도 괜찮은가

| 걱정 | 실제로는 |
|---|---|
| "언어 하나로는 부족하지 않나?" | 지금 배우는 건 언어가 아니라 **구조**(제어-수집-통계-저장이 어떻게 맞물리는지)다. 구조를 체득하면 언어는 나중에 바꿔도 된다. |
| "이거 나중에 다 버리는 거 아닌가?" | 아니다. 실제 PLC를 붙일 때도 `plc_logic.py`의 상태머신 구조는 거의 그대로 쓰고, 통신 드라이버(`pymodbus` 등)만 추가하면 된다. |
| "실무에서도 이렇게 해?" | 그렇다. 개념검증(PoC) 단계는 실무에서도 빠르게 짤 수 있는 언어로 먼저 만들어보고, 이후 정식 버전으로 확장하는 경우가 흔하다. |

---

## 3. 전체 구조 (미니 버전 아키텍처)

```
[GUI 화면 (gui.py)]
   │  버튼 클릭 (시작/정지)
   ▼
[PLC 로직 (plc_logic.py)]
   │  상태 전이 계산 (IDLE→MOVING→...→IDLE)
   ▼
[로거 (logger.py)] ──────▶ [기록 파일 (log.csv)]
   │
   ▼
[통계 (stats.py)]  ◀── log.csv 읽어서 계산
   │
   ▼
[GUI 화면에 통계 표시]
```

이 5개 조각이 전에 그린 "5계층 구조"에 각각 이렇게 대응된다:

| 미니 프로젝트 파일 | 대응되는 실제 계층 |
|---|---|
| `plc_logic.py` | PLC 로직 계층 |
| (통신 계층은 생략 — 같은 파이썬 프로세스 안에서 함수 호출로 대체) | 통신 계층 |
| `gui.py` | PC 애플리케이션 계층 (HMI) |
| `logger.py` + `log.csv` | PC 애플리케이션 계층 (데이터 로깅 / Historian) |
| `stats.py` | PC 애플리케이션 계층 (트렌드 분석 / 리포트) |

---

## 4. 미니 버전 전체 동작 시나리오 (한 사이클)

1. 사용자가 GUI에서 **시작** 버튼을 누른다.
2. `plc_logic.py`의 상태머신이 `IDLE → MOVING → DETECTED → STOPPED → PUSHING → RETURNING → IDLE` 순서로 상태를 바꾼다.
3. 상태가 바뀔 때마다 `gui.py` 화면의 "현재 상태" 글자가 즉시 갱신된다.
4. 동시에 `logger.py`가 그 상태 전이를 시간과 함께 `log.csv`에 한 줄씩 추가한다.
5. 사용자가 **통계 보기** 버튼을 누르면, `stats.py`가 `log.csv`를 읽어서:
   - 지금까지 몇 번 사이클을 돌았는지
   - 한 사이클이 평균 몇 초 걸렸는지
   - 어떤 상태에서 시간이 가장 오래 걸렸는지
   를 계산해서 GUI 화면에 숫자로 보여준다.
6. 사용자가 **정지** 버튼을 누르면, 현재 사이클만 마치고 멈춘다.

---

## 5. 파일 구조

| 파일 | 역할 | 사용 라이브러리 |
|---|---|---|
| `main.py` | 전체를 불러와서 실행하는 진입점 | (없음, 표준 파이썬) |
| `plc_logic.py` | 상태머신 (IDLE→...→IDLE) | (없음) |
| `gui.py` | 화면, 버튼, 상태 표시 | `tkinter` (파이썬 내장) |
| `logger.py` | 상태 전이를 csv에 기록 | `csv` (파이썬 내장) |
| `stats.py` | csv를 읽어서 통계 계산 | `csv` (파이썬 내장) |
| `plc_server.py` | (통신 버전) 소켓 서버 — PLC 로직을 켜두고 명령을 기다림 | `socket` (파이썬 내장) |
| `pc_client.py` | (통신 버전) 소켓 클라이언트 — 서버에 명령을 보내고 상태를 받아옴 | `socket` (파이썬 내장) |
| `log.csv` | 실행하면서 자동 생성되는 기록 파일 | - |
| `README.md` | 프로젝트 설명 | - |

**추가 설치가 필요한 라이브러리는 없다.** 전부 파이썬 설치하면 기본으로 들어있는 것들이다.

---

## 6. 행동 스텝 (체크리스트)

- [ ] **0단계** — Python 설치 확인, 프로젝트 폴더 하나 생성 (`plc_project`)
- [ ] **1단계** — `plc_logic.py` 작성: 상태머신 클래스만 (이미 만든 `plc_sim.py` 재활용 가능)
- [ ] **2단계** — `logger.py` 작성: 상태 전이를 받아서 `log.csv`에 한 줄씩 쓰는 함수
- [ ] **3단계** — `gui.py` 작성: tkinter 창 + 시작/정지 버튼 + 상태 표시 글자
- [ ] **4단계** — `stats.py` 작성: `log.csv` 읽어서 사이클 수 / 평균 시간 계산
- [ ] **5단계** — `main.py` 작성: 위 4개를 불러와서 실제로 연결 (버튼 → 로직 → 로그 → 통계 표시)
- [ ] **6단계** — 실행 테스트: 몇 번 돌려보고 `log.csv`와 통계 화면이 맞게 나오는지 확인
- [ ] **7단계** — `README.md` 작성 후 GitHub 업로드 (포트폴리오화)
- [ ] **8단계** — 통신 분리 버전 만들기: `plc_server.py` + `pc_client.py`로 쪼개서 소켓(socket) 통신 실습
- [ ] **9단계** — 배포용 실행파일 만들기: PyInstaller로 패키징해서 파이썬 없는 컴퓨터에서도 실행되게 만들기

**지금 위치: 1~5, 8단계에 해당하는 코드 파일은 모두 생성 완료. 지금부터 직접 실행해보면서 6단계(테스트)를 진행할 차례.**

---

## 7. 나중에 확장할 때 (지금은 안 해도 됨, 참고만)

| 지금 (미니 버전) | 나중에 (실전 버전) |
|---|---|
| `tkinter` 화면 | 웹 화면 (JavaScript) |
| 함수 호출로 값 전달 | API 통신 |
| `csv` 파일 저장 | 데이터베이스 (MySQL 등) |
| 파이썬 로직으로 흉내 낸 PLC | 진짜 PLC + `pymodbus` 등 통신 드라이버 |
| SCADA 개념 전체 | + 반도체는 SECS/GEM 표준까지 추가 |

이 표는 "지금 만드는 게 실전과 어떻게 이어지는지"를 나중에 다시 확인할 때만 보면 된다. 지금은 6번 체크리스트만 따라가면 된다.
